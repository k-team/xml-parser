#ifndef DOCTYPE_H
#define DOCTYPE_H

#include "pi.h"

class Doctype : public PI {
  public:
    Doctype();
    virtual ~Doctype();
    void to_be_or_not_to_be();
};

#endif // vim:ft=cpp et sw=2 sts=2:
